<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="90"
        android:endColor="@color/logo_background"
        android:centerColor="@color/logo_background"
        android:startColor="@color/md_theme_light_secondaryContainer"
        android:centerX="30%"
        android:type="linear" />
</shape>